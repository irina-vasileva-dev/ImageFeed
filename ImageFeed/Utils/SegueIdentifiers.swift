import Foundation

/// Centralized segue identifiers to avoid magic strings.
enum SegueIdentifier {
    static let showAuthentication = "ShowAuthentication"
    static let showWebView = "ShowWebView"
    static let showSingleImage = "ShowSingleImage"
}
